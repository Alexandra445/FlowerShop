# Архитектура вашего приложения

## Клиент-серверная архитектура (Desktop)

### Структура:

```
┌─────────────────┐
│   GUI (Клиент)  │ ← Desktop-приложение с кнопками
│   (Кнопки)      │
└────────┬────────┘
         │
         │ Вызывает методы
         ▼
┌─────────────────┐
│   API (Сервисы) │ ← Вспомогательная штука с методами
│                 │
│ - getAll()      │
│ - add()         │
│ - update()      │
│ - delete()      │
└────────┬────────┘
         │
         │ Обращается к
         ▼
┌─────────────────┐
│   Сервер (БД)   │ ← Сервер, который обращается с БД
│   Database      │
└────────┬────────┘
         │
         │ Работает с
         ▼
┌─────────────────┐
│   PostgreSQL    │ ← База данных
└─────────────────┘
```

---

## Компоненты:

### 1. GUI (Клиент) - `src/gui/`

**Что делает:**
- Только кнопки и интерфейс
- При нажатии кнопки вызывает методы из API

**Файлы:**
- `LoginFrame.java` - окно входа
- `FlowerPanel.java` - панель с кнопками для цветов
- `AdminMainFrame.java` - главное окно админа
- `ClientMainFrame.java` - главное окно клиента
- И т.д.

**Пример:**
```java
// FlowerPanel.java
// Админ нажимает кнопку "Добавить"
if (service.add(name.trim(), 1, quantity, price)) {
    // Цветок добавлен!
}
```

### 2. API (Сервисы) - `src/api/*Service.java`

**Что делает:**
- Хранит все методы, функции (getAll, add, update, delete)
- Кнопки обращаются к этим методам
- Эти методы обращаются к серверу (Database)

**Файлы:**
- `FlowerService.java` - методы для работы с цветами
- `OrderService.java` - методы для работы с заказами
- `ClientService.java` - методы для работы с клиентами
- `AdminService.java` - методы для работы с админами
- `BouquetService.java` - методы для работы с букетами
- И т.д.

**Пример:**
```java
// FlowerService.java
public boolean add(String name, int typeId, int quantity, BigDecimal price) {
    // Получает данные от кнопки
    // Обращается к серверу
    Connection c = Database.getConnection();
    // Подает сигнал серверу сохранить данные
    ps.executeUpdate();
}
```

### 3. Сервер (Database) - `src/server/Database.java`

**Что делает:**
- Обращается с базой данных PostgreSQL
- Предоставляет подключение к БД
- API использует этот сервер для работы с БД

**Файл:**
- `Database.java`

**Пример:**
```java
// Database.java
public static Connection getConnection() throws SQLException {
    // Подключается к базе данных
    return DriverManager.getConnection(URL, USER, PASSWORD);
}
```

### 4. База данных - PostgreSQL

**Что делает:**
- Хранит все данные (цветы, букеты, заказы, клиенты, админы)

---

## Как это работает:

### Пример: Админ добавляет цветок

1. **GUI (Клиент):**
   - Админ открывает `FlowerPanel`
   - Нажимает кнопку "Добавить"
   - Вводит данные (название, цена, количество)
   - Нажимает "Сохранить"
   - Вызывает: `service.add(name, typeId, quantity, price)`

2. **API (Сервисы):**
   - Метод `add()` в `FlowerService` получает данные от кнопки
   - Обращается к серверу: `Connection c = Database.getConnection()`
   - Подает сигнал серверу сохранить данные: `ps.executeUpdate()`

3. **Сервер (Database):**
   - Подключается к PostgreSQL
   - Выполняет SQL запрос: `INSERT INTO flowers ...`

4. **База данных (PostgreSQL):**
   - Сохраняет данные

**ВСЁ! Именно так и работает!**

---

## Структура проекта:

```
flowershop/
├── src/
│   ├── gui/              ← GUI (Клиент) - кнопки
│   │   ├── LoginFrame.java
│   │   ├── FlowerPanel.java
│   │   ├── AdminMainFrame.java
│   │   └── ...
│   │
│   ├── api/              ← API (Сервисы) - методы, функции
│   │   ├── FlowerService.java
│   │   ├── OrderService.java
│   │   ├── ClientService.java
│   │   ├── AdminService.java
│   │   └── ...
│   │
│   └── server/           ← Сервер (Database) + Модели
│       ├── Database.java         ← Сервер (БД)
│       ├── Flower.java           ← Модель
│       ├── Order.java            ← Модель
│       └── ...
│
└── PostgreSQL            ← База данных
```

---

## Итог:

**У вас классическая клиент-серверная архитектура:**

1. ✅ **GUI (Клиент)** - Desktop-приложение с кнопками
2. ✅ **API (Сервисы)** - Вспомогательная штука с методами
3. ✅ **Сервер (Database)** - Обращается с базой данных
4. ✅ **База данных (PostgreSQL)** - Хранит данные

**Связь:**
- Кнопки → обращаются к API
- API → обращается к серверу
- Сервер → работает с БД

**ВСЁ ПРАВИЛЬНО!** 🎉

