# ПОЛНОЕ ОБЪЯСНЕНИЕ КУРСОВОЙ РАБОТЫ "FLOWERSHOP"

## 🎯 ОБЩАЯ КОНЦЕПЦИЯ

Это **клиент-серверное приложение** для цветочного магазина, состоящее из трех основных компонентов:

1. **GUI Клиент** (Desktop приложение на Java Swing) - работает на компьютере пользователя
2. **Сервер** (Tomcat + Java Servlets) - работает на сервере, доступен по сети
3. **База данных** (PostgreSQL) - хранит все данные

---

## 📐 АРХИТЕКТУРА В ЦЕЛОМ

```
┌─────────────────────────────────────────────────────────────┐
│                    GUI КЛИЕНТ (Desktop)                      │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │ LoginFrame   │  │ AdminMainFrame│  │ClientMainFrame│      │
│  │              │  │              │  │              │      │
│  │ FlowerPanel  │  │ OrderPanel   │  │ CatalogPanel │      │
│  │ BouquetPanel │  │ AdminPanel   │  │ OrdersPanel  │      │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘      │
│         │                  │                  │              │
│         └──────────────────┼──────────────────┘              │
│                            │                                  │
│                    ┌───────▼────────┐                        │
│                    │  ServerClient  │                        │
│                    │  (HTTP Client) │                        │
│                    └───────┬────────┘                        │
└────────────────────────────┼─────────────────────────────────┘
                             │ HTTP (JSON)
                             │ POST/GET/PUT/DELETE
                             │ http://localhost:8080/flowershop
                             │
┌────────────────────────────▼─────────────────────────────────┐
│              TOMCAT (Сервер приложений)                       │
│                  Порт 8080                                    │
│  ┌────────────────────────────────────────────────────┐      │
│  │         web.xml (конфигурация сервлетов)            │      │
│  │  /flowers → FlowerServlet                          │      │
│  │  /bouquets → BouquetServlet                        │      │
│  │  /orders → OrderServlet                            │      │
│  │  /clients → ClientServlet                          │      │
│  │  /admins → AdminServlet                            │      │
│  │  /auth → AuthServlet                               │      │
│  │  /order-statuses → OrderStatusServlet              │      │
│  └────────────────────────────────────────────────────┘      │
│                                                               │
│  ┌────────────────────────────────────────────────────┐      │
│  │              СЕРВЛЕТЫ (server/*Servlet.java)        │      │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐│      │
│  │  │FlowerServlet│  │OrderServlet │  │AuthServlet  ││      │
│  │  │             │  │             │  │             ││      │
│  │  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘│      │
│  │         │                 │                 │        │      │
│  │         └─────────────────┼─────────────────┘        │      │
│  │                           │                           │      │
│  │                  ┌────────▼────────┐                  │      │
│  │                  │  API Сервисы    │                  │      │
│  │                  │  (api/*Service) │                  │      │
│  │                  └────────┬────────┘                  │      │
│  └───────────────────────────┼───────────────────────────┘      │
└──────────────────────────────┼──────────────────────────────────┘
                               │
                               │ JDBC
                               │
┌──────────────────────────────▼──────────────────────────────────┐
│                    PostgreSQL БАЗА ДАННЫХ                        │
│              Порт 5432, БД: flowerstore                          │
│                                                                  │
│  Таблицы:                                                        │
│  - clients (клиенты)                                             │
│  - administrators (администраторы)                               │
│  - flowers (цветы)                                               │
│  - bouquets (букеты)                                             │
│  - orders (заказы)                                               │
│  - order_statuses (статусы заказов)                              │
│  - ...                                                           │
└──────────────────────────────────────────────────────────────────┘
```

---

## 📁 СТРУКТУРА ПРОЕКТА

```
flowershop/
├── src/
│   ├── gui/                    # GUI КЛИЕНТ - интерфейс пользователя
│   │   ├── LoginFrame.java     # Окно авторизации
│   │   ├── AdminMainFrame.java # Главное окно администратора
│   │   ├── ClientMainFrame.java# Главное окно клиента
│   │   ├── FlowerPanel.java    # Панель управления цветами (админ)
│   │   ├── BouquetPanel.java   # Панель управления букетами (админ)
│   │   ├── OrderPanel.java     # Панель просмотра заказов
│   │   ├── ClientPanel.java    # Панель управления клиентами
│   │   ├── AdminPanel.java     # Панель управления админами
│   │   ├── ClientCatalogPanel.java  # Каталог для клиентов
│   │   └── ClientOrdersPanel.java   # Заказы клиента
│   │
│   ├── client/                 # КЛИЕНТСКИЙ СЛОЙ - HTTP клиент
│   │   └── ServerClient.java   # Класс для отправки HTTP запросов к серверу
│   │
│   ├── api/                    # API СЛОЙ - бизнес-логика работы с БД
│   │   ├── FlowerService.java      # Логика работы с цветами
│   │   ├── BouquetService.java     # Логика работы с букетами
│   │   ├── OrderService.java       # Логика работы с заказами
│   │   ├── ClientService.java      # Логика работы с клиентами
│   │   ├── AdminService.java       # Логика работы с админами
│   │   ├── OrderStatusService.java # Логика работы со статусами
│   │   └── FlowerTypeService.java  # Логика работы с типами цветов
│   │
│   └── server/                 # СЕРВЕРНЫЙ СЛОЙ
│       ├── *Servlet.java       # Сервлеты - обработчики HTTP запросов
│       │   ├── FlowerServlet.java
│       │   ├── OrderServlet.java
│       │   ├── AuthServlet.java
│       │   └── ...
│       │
│       ├── Database.java       # Класс для подключения к PostgreSQL
│       ├── JsonHelper.java     # Вспомогательный класс для работы с JSON
│       │
│       └── Модели данных (POJO классы):
│           ├── Flower.java         # Модель цветка
│           ├── Bouquet.java        # Модель букета
│           ├── Order.java          # Модель заказа
│           ├── Client.java         # Модель клиента
│           ├── Administrator.java  # Модель администратора
│           └── OrderStatus.java    # Модель статуса заказа
│
└── webapp/                     # WEB-ПРИЛОЖЕНИЕ для Tomcat
    ├── index.html              # Стартовая страница
    └── WEB-INF/
        └── web.xml             # Конфигурация сервлетов
```

---

## 🔄 КАК ВСЁ ВЗАИМОДЕЙСТВУЕТ

### 1. ЗАПУСК ПРИЛОЖЕНИЯ

#### Шаг 1: Запуск Tomcat сервера
- Tomcat запускается на порту 8080
- Загружает `webapp/` папку
- Читает `web.xml` и регистрирует все сервлеты
- Сервлеты доступны по адресам:
  - `http://localhost:8080/flowershop/flowers`
  - `http://localhost:8080/flowershop/orders`
  - и т.д.

#### Шаг 2: Запуск GUI клиента
```java
// LoginFrame.java, строка 94-96
public static void main(String[] args) {
    SwingUtilities.invokeLater(() -> new LoginFrame().setVisible(true));
}
```
- Запускается окно авторизации (`LoginFrame`)
- Пользователь вводит логин и пароль

---

### 2. ПРОЦЕСС АВТОРИЗАЦИИ

```
Пользователь нажимает "Войти"
    │
    ▼
LoginFrame.onLogin() вызывается
    │
    ▼
serverClient.login(login, password) 
    │
    ▼
ServerClient.sendPostRequest("/auth", json)
    │
    ├─ HTTP POST запрос
    │  URL: http://localhost:8080/flowershop/auth
    │  Body: {"login":"admin", "password":"123"}
    │
    ▼
Tomcat получает запрос на /auth
    │
    ▼
web.xml направляет запрос к AuthServlet
    │
    ▼
AuthServlet.doPost() вызывается
    │
    ├─ Читает JSON из запроса через JsonHelper
    │  LoginRequest {login, password}
    │
    ├─ Проверяет через AdminService.login()
    │  или ClientService.login()
    │
    │  ┌─────────────────────┐
    │  │ AdminService.login()│
    │  │                     │
    │  │ 1. Получает Connection│
    │  │    из Database.getConnection()
    │  │                     │
    │  │ 2. Выполняет SQL:   │
    │  │    SELECT * FROM    │
    │  │    administrators   │
    │  │    WHERE login=?    │
    │  │    AND password=?   │
    │  │                     │
    │  │ 3. Возвращает true  │
    │  │    если найден      │
    │  └─────────────────────┘
    │
    ▼
AuthServlet формирует JSON ответ
    │
    ├─ Если успешно:
    │  {"success":true, "userType":"admin"}
    │  или
    │  {"success":true, "userType":"client", "clientId":5}
    │
    ├─ Если ошибка:
    │  {"success":false, "error":"Неверный логин или пароль"}
    │
    ▼
HTTP Response отправляется клиенту
    │
    ▼
ServerClient получает ответ, парсит JSON
    │
    ├─ Gson преобразует JSON в LoginResponse объект
    │
    ▼
LoginFrame получает LoginResponse
    │
    ├─ Если response.success == true:
    │  ├─ Если userType == "admin":
    │  │  └─ Открывает AdminMainFrame
    │  │
    │  └─ Если userType == "client":
    │     └─ Открывает ClientMainFrame(clientId)
    │
    └─ Если response.success == false:
       └─ Показывает ошибку
```

---

### 3. ПРИМЕР: АДМИНИСТРАТОР ДОБАВЛЯЕТ ЦВЕТОК

```
Админ в AdminMainFrame → вкладка "Цветы" → FlowerPanel
    │
    ├─ Нажимает кнопку "Добавить"
    │
    ▼
FlowerPanel.addFlower() вызывается
    │
    ├─ Показывает диалог ввода:
    │  - Название цветка
    │  - Количество
    │  - Цена
    │
    ▼
serverClient.addFlower(name, typeId, quantity, price)
    │
    ├─ Создает FlowerRequest объект:
    │  {
    │    "name": "Роза",
    │    "typeId": 1,
    │    "quantity": 50,
    │    "price": 150.00
    │  }
    │
    ├─ Преобразует в JSON через Gson:
    │  String json = gson.toJson(request)
    │
    ▼
ServerClient.sendPostRequest("/flowers", json)
    │
    ├─ HTTP POST запрос:
    │  URL: http://localhost:8080/flowershop/flowers
    │  Method: POST
    │  Headers: Content-Type: application/json
    │  Body: {"name":"Роза", "typeId":1, "quantity":50, "price":150.00}
    │
    ▼
Tomcat получает POST на /flowers
    │
    ▼
web.xml направляет к FlowerServlet
    │
    ▼
FlowerServlet.doPost() вызывается
    │
    ├─ 1. Устанавливает Content-Type: application/json
    │  2. Получает PrintWriter для ответа
    │  3. Читает JSON из тела запроса:
    │     String json = JsonHelper.readJsonFromRequest(req.getReader())
    │  4. Парсит JSON в FlowerRequest:
    │     FlowerRequest request = JsonHelper.fromJson(json, FlowerRequest.class)
    │
    ▼
flowerService.add(...) вызывается
    │  (api.FlowerService)
    │
    ├─ Создает SQL запрос:
    │  INSERT INTO flowers (name, type_id, quantity, price)
    │  VALUES (?, ?, ?, ?)
    │
    ├─ Получает Connection:
    │  Connection c = Database.getConnection()
    │
    │  ┌─────────────────────┐
    │  │  Database.getConnection() │
    │  │                     │
    │  │  1. Загружает       │
    │  │     PostgreSQL Driver│
    │  │                     │
    │  │  2. Подключается:   │
    │  │     jdbc:postgresql://│
    │  │     localhost:5432/  │
    │  │     flowerstore      │
    │  │                     │
    │  │  3. Возвращает      │
    │  │     Connection      │
    │  └─────────────────────┘
    │
    ├─ Выполняет PreparedStatement:
    │  ps.setString(1, name)
    │  ps.setInt(2, typeId)
    │  ps.setInt(3, quantity)
    │  ps.setBigDecimal(4, price)
    │  ps.executeUpdate()
    │
    ├─ Возвращает true если успешно (1 строка изменена)
    │
    ▼
FlowerServlet формирует JSON ответ:
    │
    ├─ Если success == true:
    │  String json = JsonHelper.successJson("Цветок успешно добавлен")
    │  Результат: {"success":"Цветок успешно добавлен"}
    │
    ├─ Устанавливает HTTP статус: SC_CREATED (201)
    │
    ├─ Отправляет ответ:
    │  out.print(json)
    │
    ▼
HTTP Response отправляется клиенту
    │
    │  Status: 201 Created
    │  Body: {"success":"Цветок успешно добавлен"}
    │
    ▼
ServerClient получает ответ
    │
    ├─ Читает строку ответа
    ├─ Проверяет: json.contains("success")
    │
    ▼
Возвращает true
    │
    ▼
FlowerPanel получает true
    │
    ├─ Показывает сообщение: "Цветок добавлен!"
    ├─ Вызывает refresh() для обновления таблицы
    │
    ▼
refresh() вызывает serverClient.getAllFlowers()
    │
    ├─ HTTP GET: http://localhost:8080/flowershop/flowers
    │
    ├─ FlowerServlet.doGet() возвращает JSON массив:
    │  [
    │    {"id":1, "name":"Роза", "quantity":50, "price":150.00},
    │    {"id":2, "name":"Тюльпан", "quantity":30, "price":80.00}
    │  ]
    │
    ├─ Gson парсит JSON в массив Flower[]
    │
    ├─ Таблица обновляется с новыми данными
```

---

## 📦 ДЕТАЛЬНОЕ ОПИСАНИЕ КОМПОНЕНТОВ

### 1. GUI СЛОЙ (src/gui/)

**Назначение:** Интерфейс пользователя на Java Swing.

#### LoginFrame.java
- **Роль:** Точка входа в приложение
- **Функции:**
  - Поля ввода логина и пароля
  - Кнопка "Войти"
  - При успешной авторизации:
    - Админ → `AdminMainFrame`
    - Клиент → `ClientMainFrame`
- **Ключевой метод:**
  ```java
  private void onLogin() {
      ServerClient.LoginResponse response = serverClient.login(login, password);
      // Обработка ответа
  }
  ```

#### AdminMainFrame.java
- **Роль:** Главное окно администратора
- **Содержит:**
  - Вкладки: "Цветы", "Букеты", "Заказы"
  - Кнопка "Выход" (возврат к LoginFrame)
- **Панели:**
  - `FlowerPanel` - управление цветами
  - `BouquetPanel` - управление букетами
  - `AdminOrdersPanel` - просмотр и управление заказами

#### ClientMainFrame.java
- **Роль:** Главное окно клиента
- **Принимает:** `Client client` (с ID клиента)
- **Содержит:**
  - Вкладки: "Каталог", "Мои заказы"
- **Панели:**
  - `ClientCatalogPanel(clientId)` - просмотр и заказ цветов/букетов
  - `ClientOrdersPanel(clientId)` - просмотр своих заказов

#### FlowerPanel.java (Админ)
- **Роль:** Управление цветами для администратора
- **Функции:**
  - Просмотр списка цветов (таблица)
  - Добавление цветка (`addFlower()`)
  - Редактирование цветка (`editFlower()`)
  - Удаление цветка (`deleteFlower()`)
  - Обновление списка (`refresh()`)
- **Использует:** `ServerClient` для всех операций

#### ClientCatalogPanel.java
- **Роль:** Каталог товаров для клиента
- **Содержит:**
  - Вкладки: "Цветы", "Букеты"
  - Таблицы с товарами
  - Кнопка "Заказать" для создания заказа
- **При заказе:**
  ```java
  serverClient.addOrder(clientId, null, flowerId, 0, quantity, 
                       statusId, null, totalPrice, deliveryTime)
  ```

---

### 2. CLIENT СЛОЙ (src/client/)

**Назначение:** HTTP клиент для общения с сервером.

#### ServerClient.java
- **Роль:** Единственная точка взаимодействия GUI с сервером
- **Базовый URL:** `http://localhost:8080/flowershop`
- **Технологии:**
  - `HttpURLConnection` для HTTP запросов
  - `Gson` для сериализации/десериализации JSON

**Ключевые методы:**

1. **sendGetRequest(path)** - GET запрос
   ```java
   URL url = URI.create(SERVER_URL + path).toURL();
   HttpURLConnection conn = (HttpURLConnection) url.openConnection();
   conn.setRequestMethod("GET");
   // Читает ответ и возвращает строку JSON
   ```

2. **sendPostRequest(path, json)** - POST запрос
   ```java
   conn.setRequestMethod("POST");
   conn.setDoOutput(true);
   // Записывает JSON в тело запроса
   ```

3. **sendPutRequest(path, json)** - PUT запрос
4. **sendDeleteRequest(path)** - DELETE запрос

**Методы для работы с данными:**
- `getAllFlowers()` → GET `/flowers`
- `addFlower(...)` → POST `/flowers`
- `updateFlower(...)` → PUT `/flowers?id=...`
- `deleteFlower(...)` → DELETE `/flowers?id=...`
- `getAllOrders()` → GET `/orders`
- `getOrdersByClient(id)` → GET `/orders?clientId=...`
- `addOrder(...)` → POST `/orders`
- `login(login, password)` → POST `/auth`

---

### 3. SERVER СЛОЙ (src/server/)

**Назначение:** Обработка HTTP запросов от клиента.

#### Сервлеты (*Servlet.java)

**Что такое сервлет?**
- Это Java класс, который обрабатывает HTTP запросы
- Наследуется от `HttpServlet`
- Методы: `doGet()`, `doPost()`, `doPut()`, `doDelete()`

#### FlowerServlet.java
- **URL:** `/flowers`
- **Методы:**
  - `doGet()` - получить все цветы
  - `doPost()` - добавить цветок
  - `doPut()` - обновить цветок
  - `doDelete()` - удалить цветок

**Структура doPost():**
```java
protected void doPost(HttpServletRequest req, HttpServletResponse resp) {
    // 1. Устанавливаем тип ответа
    resp.setContentType("application/json; charset=UTF-8");
    PrintWriter out = resp.getWriter();
    
    try {
        // 2. Читаем JSON из тела запроса
        String json = JsonHelper.readJsonFromRequest(req.getReader());
        
        // 3. Парсим JSON в объект
        FlowerRequest request = JsonHelper.fromJson(json, FlowerRequest.class);
        
        // 4. Вызываем сервис (бизнес-логика)
        boolean success = flowerService.add(...);
        
        // 5. Формируем JSON ответ
        if (success) {
            out.print(JsonHelper.successJson("Цветок успешно добавлен"));
            resp.setStatus(HttpServletResponse.SC_CREATED); // 201
        } else {
            out.print(JsonHelper.errorJson("Ошибка"));
            resp.setStatus(HttpServletResponse.SC_INTERNAL_SERVER_ERROR); // 500
        }
    } catch (Exception e) {
        // Обработка ошибок
    }
}
```

#### OrderServlet.java
- **URL:** `/orders`
- **doGet():**
  - Если есть параметр `clientId` → заказы конкретного клиента
  - Иначе → все заказы
- **doPost():** Создание нового заказа
- **doPut():** Обновление статуса заказа

#### AuthServlet.java
- **URL:** `/auth`
- **doPost():** Авторизация
  - Проверяет через `AdminService.login()`
  - Если не админ, проверяет через `ClientService.login()`
  - Возвращает JSON с информацией о пользователе

#### JsonHelper.java
- **Роль:** Утилиты для работы с JSON
- **Методы:**
  - `toJson(obj)` - объект → JSON строка
  - `fromJson(json, Class)` - JSON строка → объект
  - `readJsonFromRequest(reader)` - читает JSON из HTTP запроса
  - `successJson(message)` - создает JSON ответ об успехе
  - `errorJson(message)` - создает JSON ответ об ошибке

#### Database.java
- **Роль:** Подключение к PostgreSQL
- **Параметры:**
  - URL: `jdbc:postgresql://localhost:5432/flowerstore`
  - User: `postgres`
  - Password: `postgres123`
- **Метод:** `getConnection()` - возвращает Connection к БД
- **Статический блок:** Загружает драйвер PostgreSQL

---

### 4. API СЛОЙ (src/api/)

**Назначение:** Бизнес-логика работы с базой данных.

**Важно:** Сервлеты используют сервисы из пакета `api`, а НЕ из `server`!

#### FlowerService.java
- **Методы:**
  - `getAll()` - получить все цветы из БД
  - `add(name, typeId, quantity, price)` - добавить цветок
  - `update(id, name, quantity, price)` - обновить цветок
  - `delete(id)` - удалить цветок (проверяет использование в заказах)
  - `reduceQuantity(id, amount)` - уменьшить количество на складе

**Пример getAll():**
```java
public List<Flower> getAll() {
    List<Flower> list = new ArrayList<>();
    String sql = "SELECT * FROM flowers ORDER BY id";
    
    try (Connection c = Database.getConnection();
         PreparedStatement ps = c.prepareStatement(sql);
         ResultSet rs = ps.executeQuery()) {
        
        while (rs.next()) {
            Flower flower = new Flower(
                rs.getInt("id"),
                rs.getString("name"),
                rs.getInt("type_id"),
                rs.getInt("quantity"),
                rs.getBigDecimal("price")
            );
            list.add(flower);
        }
    } catch (SQLException e) {
        e.printStackTrace();
    }
    return list;
}
```

#### OrderService.java
- **Особенность:** Использует сложные JOIN запросы для получения дополнительной информации
- **getAll():**
  ```sql
  SELECT o.id, o.client_id, o.bouquet_id, o.flower_id, 
         o.status_id, o.order_date, o.delivery_time,
         o.bouquet_count, o.flower_count, o.administrator_id, 
         o.total_price,
         b.name AS bouquet_name,
         f.name AS flower_name,
         s.name AS status_name,
         c.full_name AS client_name,
         c.phone AS client_phone
  FROM orders o
  LEFT JOIN bouquets b ON o.bouquet_id = b.id
  LEFT JOIN flowers f ON o.flower_id = f.id
  LEFT JOIN order_statuses s ON o.status_id = s.id
  LEFT JOIN clients c ON o.client_id = c.id
  ORDER BY o.id
  ```
- **add():** Создает заказ и уменьшает количество цветов на складе
- **updateStatus():** Изменяет статус заказа

---

### 5. МОДЕЛИ ДАННЫХ (src/server/*.java)

**Назначение:** Представление данных из БД в виде Java объектов (POJO).

#### Order.java
- **Поля:**
  - Основные: `id`, `clientId`, `bouquetId`, `flowerId`, `statusId`, etc.
  - Для отображения: `bouquetName`, `flowerName`, `statusName`, `clientName`, `clientPhone`
- **Конструктор:** Принимает все 15 параметров
- **Геттеры:** Для всех полей (нет сеттеров - объекты immutable)

#### Flower.java, Bouquet.java, Client.java
- Аналогичная структура
- Геттеры/сеттеры для всех полей

---

### 6. КОНФИГУРАЦИЯ (webapp/WEB-INF/web.xml)

**Назначение:** Регистрация сервлетов в Tomcat.

**Структура:**
```xml
<servlet>
    <servlet-name>FlowerServlet</servlet-name>
    <servlet-class>server.FlowerServlet</servlet-class>
</servlet>
<servlet-mapping>
    <servlet-name>FlowerServlet</servlet-name>
    <url-pattern>/flowers</url-pattern>
</servlet-mapping>
```

Это говорит Tomcat:
- Когда приходит запрос на `/flowers` → вызвать `server.FlowerServlet`

---

## 🔍 ВАЖНЫЕ ДЕТАЛИ И ТОНКОСТИ

### 1. Почему два пакета сервисов (api и server)?

**api/*Service.java:**
- Используются **сервлетами**
- Содержат полную бизнес-логику
- Работают с JOIN запросами для получения связанных данных

**server/*Service.java:**
- **Старые версии** или дубликаты (не используются в текущей архитектуре)
- Остались от предыдущих версий проекта

**Правильный путь:** Сервлет → api сервис → Database → PostgreSQL

### 2. Формат данных: JSON

**Зачем JSON?**
- Универсальный формат обмена данными
- Легко парсить на любом языке
- Текстовый формат (читаемый)

**Пример запроса:**
```json
POST /flowershop/flowers
Content-Type: application/json

{
  "name": "Роза",
  "typeId": 1,
  "quantity": 50,
  "price": 150.00
}
```

**Пример ответа:**
```json
{
  "success": "Цветок успешно добавлен"
}
```

### 3. HTTP методы

- **GET** - получение данных (безопасный, кэшируемый)
- **POST** - создание нового ресурса
- **PUT** - обновление существующего ресурса
- **DELETE** - удаление ресурса

### 4. HTTP статус коды

- **200 OK** - успешный запрос
- **201 Created** - ресурс создан
- **400 Bad Request** - неверный запрос
- **401 Unauthorized** - не авторизован
- **404 Not Found** - ресурс не найден
- **500 Internal Server Error** - ошибка сервера

### 5. Авторизация

**Процесс:**
1. Клиент отправляет логин/пароль
2. Сервер проверяет в БД
3. Если найден - возвращает тип пользователя (admin/client) и ID
4. Клиент сохраняет эту информацию и использует для дальнейших запросов

**Проблема безопасности:** Пароли хранятся в открытом виде! В реальном приложении нужно хэширование.

### 6. Обработка ошибок

**На клиенте:**
- Try-catch блоки
- Возврат пустых списков при ошибках
- Логирование в консоль (`e.printStackTrace()`)

**На сервере:**
- Try-catch в сервлетах
- Возврат JSON с описанием ошибки
- Установка соответствующего HTTP статуса

### 7. Создание заказа

**Логика:**
1. Клиент выбирает цветы/букеты
2. Указывает количество
3. Выбирает время доставки
4. `OrderService.add()` выполняет:
   - INSERT в таблицу orders
   - Уменьшает количество цветов на складе (`reduceQuantity()`)

**Важно:** Бизнес-правила проверяются в сервисе, а не в GUI!

---

## 🚀 КАК ЗАПУСТИТЬ ПРОЕКТ

### 1. Подготовка

1. **Установить PostgreSQL**
   - Создать БД: `flowerstore`
   - Пользователь: `postgres`
   - Пароль: `postgres123`

2. **Установить Tomcat**
   - Версия 9.0+
   - Порт: 8080

### 2. Сборка проекта

1. Скомпилировать Java классы
2. Упаковать серверную часть в WAR файл
3. Поместить WAR в `webapps/` папку Tomcat

### 3. Запуск

1. **Запустить PostgreSQL**
2. **Запустить Tomcat**
   - Должен слушать порт 8080
3. **Запустить GUI клиент:**
   ```java
   LoginFrame.main(args)
   ```

---

## 📝 ЧТО НУЖНО ЗНАТЬ ДЛЯ ЗАЩИТЫ

1. **Архитектура:**
   - Трехзвенная архитектура (клиент-сервер-БД)
   - Роль каждого слоя
   - Как данные передаются между слоями

2. **Технологии:**
   - Java Swing (GUI)
   - Java Servlets (сервер)
   - PostgreSQL JDBC (БД)
   - JSON (формат данных)
   - HTTP (протокол)
   - Tomcat (контейнер сервлетов)

3. **Основные классы:**
   - `ServerClient` - как работает HTTP клиент
   - Сервлеты - как обрабатываются запросы
   - Сервисы - бизнес-логика
   - `Database` - подключение к БД

4. **Процессы:**
   - Авторизация (полный цикл)
   - Добавление товара (полный цикл)
   - Создание заказа (полный цикл)

5. **Структура БД:**
   - Какие таблицы есть
   - Связи между таблицами (FK)
   - JOIN запросы

---

## 🎓 ВОПРОСЫ ДЛЯ САМОПРОВЕРКИ

1. Что происходит, когда пользователь нажимает "Добавить цветок"?
2. Как клиент узнает, что он админ или клиент?
3. Где выполняется SQL запрос?
4. Что такое сервлет и зачем он нужен?
5. Почему используется JSON, а не XML?
6. Что будет, если Tomcat не запущен, а клиент пытается отправить запрос?
7. Как клиент получает данные о заказах?
8. В чем разница между GET и POST запросами?
9. Как работает JOIN в SQL запросе OrderService.getAll()?
10. Что делает JsonHelper?

---

## ✨ ЗАКЛЮЧЕНИЕ

Это полноценное клиент-серверное приложение с разделением ответственности:

- **GUI** - только интерфейс, не знает о БД
- **ServerClient** - только HTTP коммуникация
- **Сервлеты** - только обработка HTTP запросов
- **Сервисы** - бизнес-логика и работа с БД
- **Database** - только подключение
- **PostgreSQL** - только хранение данных

Каждый слой имеет четкую ответственность, что делает код поддерживаемым и масштабируемым.
